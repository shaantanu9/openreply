# Manual TODO — activation-suite

Index of human-action items (dashboard clicks, env vars, real-device checks)
that code/CLI can't do automatically.

| File | Topic | Status |
|---|---|---|
| `download-links.md` | Wiring site downloads to GitHub releases | ✅ working — only optional items remain |

When all items in a file are checked, move it to `docs/manual-todo/done/`.
