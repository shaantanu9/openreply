# Release v0.1.7 — merge feature branch + tag + trigger signed CI release

**Date:** 2026-05-31
**Type:** Infrastructure (Release)

## Summary

Consolidated all outstanding work onto the `multi-source` release branch and cut
release **v0.1.7**. The `feat/merge-topics` branch (carrying the MCP "Copy config"
button + `mcp_config_snippet` command) was merged into `multi-source`, which already
held the topic-merge feature, the WAL data-loss fix, test-isolation hardening, and the
security/audit fixes. After resolving a merge conflict in `commands.rs`, verifying the
Rust crate compiles, and passing `scripts/preflight-release.sh`, the `v0.1.7` annotated
tag was pushed to `origin` (`shaantanu9/openreply`), triggering the signed
mac/windows/linux release CI builds.

## Changes

- Merged `feat/merge-topics` → `multi-source` with a no-ff merge commit (`dfc5f05`).
- Resolved a content conflict in `app-tauri/src-tauri/src/commands.rs` in the
  `mcp_config_snippet` command: kept the `feat/merge-topics` side for both the doc
  comment and the ephemeral-path guard, because its guard distinguishes
  AppTranslocation / `/Volumes` / temporary paths — matching the sibling `mcp_install`
  guard already in the file. Verified a single `fn mcp_config_snippet` definition and a
  single registration in `main.rs` (no duplicate handler).
- Verified `cargo check` passes on `app-tauri/src-tauri` (only the benign
  `JWT_DESKTOP_SECRET` debug-fallback warning).
- Ran `scripts/preflight-release.sh v0.1.7` — all checks green (version pins across
  tauri.conf.json / package.json / Cargo.toml / pyproject.toml all 0.1.7, tag free,
  clean tree on release branch, Apple signing creds + Developer ID cert present).
- Pushed `multi-source` (`81cb523..dfc5f05`).
- Created and pushed annotated tag `v0.1.7` (`78613d8`) → triggered
  `release (mac)`, `release (windows)`, `release (linux)` CI workflows.

## Files Created

- `changelogs/2026-05-31_15_release-v0.1.7.md`

## Files Modified

- `app-tauri/src-tauri/src/commands.rs` — merge-conflict resolution in
  `mcp_config_snippet` (doc comment + ephemeral-path guard taken from `feat/merge-topics`).

## Notes

- The pre-push hook printed a "preflight failed" abort message on the tag push; this is
  the known false-positive — the hook re-runs preflight which sees the tag now exists
  (it is the very tag being pushed). The tag push completed; confirmed `v0.1.7` on origin.
- The GitHub Release object is created as a **draft** by tauri-action once builds finish.
  Publishing (flip `--draft=false --latest`) and the section-11 signed-DMG verification
  are follow-ups once CI is green.
- Cleanup pending (from prior session): worktree at `../reddit-myind-merge`
  (`feat/merge-topics-wt`) can be removed with `git worktree remove` when satisfied.
